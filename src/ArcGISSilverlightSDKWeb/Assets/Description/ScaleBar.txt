This sample shows how to associate a ScaleBar control with a map.  Element binding is used to bind the ScaleBar and other elements to the map and its contents in XAML.  The copyright text for the base layer is also included below the ScaleBar.  The Map's control template is explicitly defined to organize and structure content relative the map and override other properties, such as the zoom box color and fill.                